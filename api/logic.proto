syntax = "proto3";

package logic;

option go_package = "./logic;logic";

message AuthReq {
  string uid = 1;
  string token = 2;
}

message AuthResp {
  bool ok = 1;
  string msg = 2;
}

service Auth {
  rpc Auth(AuthReq) returns (AuthResp);
}